// Placeholder for API Configuration
export const API_CONFIG = {
  // Use EXPO_PUBLIC_ prefix for Expo environment variables
  BASE_URL: process.env.EXPO_PUBLIC_API_BASE_URL || 'https://api.zamin24.example.com/v1',
  TIMEOUT: 10000,
};
